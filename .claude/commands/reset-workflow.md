Reset the development workflow to start fresh.

1. Delete `.claude/workflow-state.json` if it exists.
2. Confirm the workflow has been reset.
3. Inform the user they can start a new workflow with `/project:new-feature "<description>"`.

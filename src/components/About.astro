---
import { Image } from 'astro:assets';
---

<section id="about" aria-labelledby="about-title" class="bg-black py-24 text-white">
  <div
    class="container mx-auto flex flex-col-reverse items-center gap-12 px-6 md:flex-row md:gap-20"
  >
    <div class="animate-fade-in-left flex-1">
      <h2 id="about-title" class="mb-6 text-4xl font-bold text-green-400">About Me</h2>
      <p class="mb-6 leading-relaxed text-gray-300">
        I’m Juan Almeida, a Full Stack Engineer passionate about creating modern, accessible, and
        AI-enhanced web experiences. My work spans front-end design, back-end architecture, and
        machine learning integration.
      </p>
      <div class="flex flex-wrap gap-3">
        {
          ['JavaScript', 'TypeScript', 'React', 'Astro', 'Python', 'Django'].map(t => (
            <span class="rounded-md border border-green-800 bg-gray-900/60 px-3 py-1 text-sm text-green-300">
              {t}
            </span>
          ))
        }
      </div>
    </div>

    <div class="animate-fade-in-right flex flex-1 justify-center">
      <Image
        src="/me.jpg"
        alt="Portrait of Juan Almeida"
        width={400}
        height={300}
        sizes="(max-width: 768px) 100vw, 480px"
        format="webp"
        quality={80}
        class="size-64 rounded-full border-4 border-green-500 shadow-lg shadow-green-500/20"
      />
    </div>
  </div>
</section>

<style is:global>
  @keyframes fade-in-left {
    from {
      opacity: 0;
      transform: translateX(-20px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }
  @keyframes fade-in-right {
    from {
      opacity: 0;
      transform: translateX(20px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }
  .animate-fade-in-left {
    animation: fade-in-left 1s ease-out forwards;
  }
  .animate-fade-in-right {
    animation: fade-in-right 1s ease-out forwards;
  }
</style>

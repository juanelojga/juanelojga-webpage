<nav
  class="fixed top-0 z-50 w-full border-b border-green-900 bg-black/40 backdrop-blur-md"
  aria-label="Main navigation"
>
  <div class="mx-auto flex max-w-6xl items-center justify-between px-6 py-4 text-green-400">
    <!-- Logo / Name -->
    <a
      href="#"
      class="text-lg font-bold tracking-wide text-green-400 drop-shadow-[0_0_12px_#00FF80] transition hover:text-green-300"
    >
      Juan Almeida
    </a>

    <!-- Desktop Nav -->
    <div class="hidden space-x-8 text-sm font-medium md:flex">
      <a href="#about" class="transition hover:text-green-300">About</a>
      <a href="#projects" class="transition hover:text-green-300">Projects</a>
      <a href="#contact" class="transition hover:text-green-300">Contact</a>
    </div>

    <!-- Mobile Menu Button -->
    <button
      id="menu-toggle"
      class="text-green-400 focus:outline-none md:hidden"
      aria-label="Toggle menu"
    >
      ☰
    </button>
  </div>

  <!-- Mobile Menu -->
  <div
    id="mobile-menu"
    class="hidden flex-col items-center space-y-4 border-t border-green-900 bg-black/90 py-4 text-green-400 md:hidden"
  >
    <a href="#about" class="transition hover:text-green-300">About</a>
    <a href="#projects" class="transition hover:text-green-300">Projects</a>
    <a href="#contact" class="transition hover:text-green-300">Contact</a>
  </div>

  <script is:inline>
    const toggle = document.getElementById('menu-toggle');
    const menu = document.getElementById('mobile-menu');
    if (toggle && menu) {
      toggle.addEventListener('click', () => {
        menu.classList.toggle('hidden');
      });
    }
  </script>

  <style>
    html {
      scroll-behavior: smooth;
    }
  </style>
</nav>
